version: 2
jobs:
  test:
    docker:
      - image: circleci/golang:1.14

    working_directory: /go/src/github.com/upfluence/pkg
    steps:
      - checkout
      - run: go test -v ./...

workflows:
  version: 2
  all:
    jobs:
      - test
